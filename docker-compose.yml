#version: '3.8'
services:
#  mysqldb:
#    image: mysql
#    volumes:
#      - ./data/db:/var/lib/mysql_ec/data
#    ports:
#      - "3306:3306"
#    environment:
#      - MYSQL_ROOT_PASSWORD=root
#      - MYSQL_DATABASE=A12.2312334g
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - app-network
networks:
  app-network:
    driver: bridge